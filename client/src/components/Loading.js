import ReactLoading from 'react-loading';

const Loading = () => {
    return(
        <ReactLoading className="m-auto" type="bars" color="#f5385d" height={100} width={100} />
    );
}

export default Loading;